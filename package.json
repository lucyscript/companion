{
  "name": "companion",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c green,blue \"npm run dev --workspace @companion/server\" \"npm run dev --workspace @companion/web\"",
    "dev:web": "npm run dev --workspace @companion/web",
    "dev:server": "npm run dev --workspace @companion/server",
    "build": "npm run build --workspace @companion/server && npm run build --workspace @companion/web",
    "typecheck": "npm run typecheck --workspace @companion/server && npm run typecheck --workspace @companion/web",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "concurrently": "^9.1.2"
  }
}
